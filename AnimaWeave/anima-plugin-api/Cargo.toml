[package]
name = "anima-plugin-api"
version = "0.1.0"
edition = "2021"
description = "Pure plugin API for AnimaWeave - zero dependencies"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 绝对不依赖核心引擎，保持完全独立 